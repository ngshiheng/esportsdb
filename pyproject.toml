[project]
name = "esportsdb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["backoff>=2.2", "hishel>=1.2", "httpx>=0.27"]

[tool.uv]
dev-dependencies = ["pytest>=8.0"]
